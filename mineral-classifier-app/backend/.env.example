# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
API_RELOAD=true
LOG_LEVEL=INFO

# Canonical data directory (mineral_classes.json, minerals.json, ...)
# Defaults to the repo-root data/; only set this if you relocate it.
# CANONICAL_DATA_DIR=../../data
